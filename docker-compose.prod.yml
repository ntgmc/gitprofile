services:
  gitprofile:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BASE_PATH: /
    image: gitprofile:latest
    restart: unless-stopped
    ports:
      - "8080:80"
